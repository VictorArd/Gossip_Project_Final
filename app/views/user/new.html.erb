
<br>

<h2 class="display-4 text-center">Créez votre compte</h2>
<p class="lead text-center">
  ici vous pouvez facilement créer votre propre compte.
</p>

<hr width="75%" color=white>

<br>

<div class="container">
    <%= form_tag url_for(action: 'create'), method: "post" do %>

        <%= label_tag 'Prénom' %>
        <%= text_field_tag 'first_name', @user.first_name, :class => "form-control"%>
<br>

        <%= label_tag 'Nom' %>
        <%= text_field_tag 'last_name', @user.last_name, :class => "form-control"%>
<br>
        <%= label_tag 'E-mail' %>
        <%= text_field_tag 'email', @user.email, :class => "form-control"%>
<br>
        <%= label_tag 'Description (optionel)' %>
        <%= text_area_tag 'description', @user.description, :class => "form-control"%>
<br>
        <%= label_tag 'Age (optionel)' %>
        <%= text_field_tag 'age', @user.age, :class => "form-control"%>
<br>

        <%= label_tag 'Ville' %>
        <%= select_tag 'city', options_for_select(@cities.collect{ |c| [c.name, c.id] })%>
<br><br>
        <%= label_tag 'Mot de passe' %>
        <%= password_field 'password', @user.password, :class => "form-control"%>
<br>
        <%= label_tag 'Confirmer votre mot de passe'%>
        <%= password_field 'password_confirmation', @user.password_confirmation, :class => "form-control"%>
<br>
<br>

      <%= submit_tag "Créer mon compte!", :class => "btn btn-info"%>
    <% end %>
</div>

<% if @user.errors.any? %>
  <p>Nous n'avons pas réussi à créer le profile pour la (ou les) raison(s) suivante(s) :</p>
  <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
